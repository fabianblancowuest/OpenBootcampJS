const alturaCM = 177;
const alturaMT = 1.77;

let peso = 97.5;
const alturaRedondeada = Math.round(alturaMT);

let pesoRedondeado = Math.floor(peso);

let maxVal = Number.MIN_VALUE + 1 === Number.MIN_VALUE;
